export type ThemeIconsProps = {
  className?: string;
  onClick?: () => void;
};

export type EmailTemplateProps = {
  name: string;
  email: string;
  subject: string;
  message: string;
};

export type searchResultsProps = {
  name: string;
  id: number;
  image?: string;
};
